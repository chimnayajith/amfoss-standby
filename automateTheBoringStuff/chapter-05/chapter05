1. {}

2. {"foo" : 42}

3. list is ordered and dictionary is unordered.

4. KeyError

5. both are the same. both check the key 

6. 'cat' in spam checks if a key named 'cat' exists whereas 'cat' in spam.values() checks if there is a value 'cat'.

7. spam.setdefault('color' , 'black')

8. ppprint.pprint()


